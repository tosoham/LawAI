# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

# LLM Provider - AIML API (OpenAI-compatible endpoint)
openai>=1.55.0

# Agent Framework
langgraph>=0.2.0

# Vector Database & Embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0
numpy>=1.26.0

# Document Processing
pypdf>=5.0.0
python-docx>=1.1.0
pdfplumber>=0.11.0

# Legal Data Ingestion (official PDFs + judgement scraping)
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Utilities
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing
python-dotenv>=1.0.0  # Environment variables
aiofiles>=24.1.0  # Async file operations
colorama>=0.4.6  # Colored terminal output

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0
httpx>=0.27.0  # For testing FastAPI

# Code Quality
ruff>=0.8.0
mypy>=1.13.0
black>=24.10.0

# Monitoring & Logging
python-json-logger>=3.1.0

# MCP server (backend/mcp_server.py). 2.x renamed FastMCP to MCPServer.
mcp>=2.0

# Accounts and conversation storage.
sqlalchemy>=2.0
# Postgres driver. SQLite is the default; DATABASE_URL points at Postgres
# where the disk is ephemeral -- a free Hugging Face Space, for one.
psycopg[binary]>=3.1
google-auth>=2.0
itsdangerous>=2.0
